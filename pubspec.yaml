name: adaptive_icons
description: A series of icons that adapt to the current platform's look.

version: 1.0.1

homepage: https://github.com/zippa-pizza/adaptive_icons
repository: https://github.com/zippa-pizza/adaptive_icons

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_adaptive: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
